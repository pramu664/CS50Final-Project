-- Code After Seperating the player and animation controller

if arg[2] == "debug" then
    require("lldebugger").start()
end

function love.load()
    Object = require "classic"
    player = require "player"
    love.graphics.setDefaultFilter("nearest", "nearest")
    player = Player()
end

function love.update(dt)
    player:update(dt)
end

function love.draw()
    player:draw()
end
